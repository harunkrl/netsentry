/* KPortWatch — Per-screen / per-widget overrides.
 *
 * Base theme (dark/nord/solarized/light) is generated at runtime by
 * ``tui.themes.apply_theme()``.  This file only contains overrides
 * that are *not* theme-dependent (layout, spacing, animations).
 */

/* Screen-level layout is handled by themes.py — nothing here. */
